import React from 'react';
import AppointmentsModeScreen from '../../components/AppointmentsModeScreen';

export default function ChainDetailsScreen() {
  return (
    <AppointmentsModeScreen
      modeKey="chain_label"
      screenKey="screen_details"
    />
  );
}
