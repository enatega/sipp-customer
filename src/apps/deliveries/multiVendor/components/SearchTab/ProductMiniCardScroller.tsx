import React from 'react';
import DeliveryProductMiniCardScroller from '../../../components/search/ProductMiniCardScroller';
import type { ProductMiniCardScrollerProps } from '../../../components/search/types';

export default function ProductMiniCardScroller(
  props: ProductMiniCardScrollerProps,
) {
  return <DeliveryProductMiniCardScroller {...props} />;
}
